DATABASE_URL=
CRON_SECRET=
# Optional: embeds new or changed signals and stable topics during ingestion.
OPENAI_API_KEY=
# Optional: raises GitHub API rate limits for server-side ingestion.
GITHUB_TOKEN=
