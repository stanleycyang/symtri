DATABASE_URL=
CRON_SECRET=
# Optional for local AI Gateway calls. Vercel deployments use OIDC automatically.
AI_GATEWAY_API_KEY=
# Optional model overrides; embeddings must support 256-dimensional output.
SYMTRI_EMBEDDING_MODEL=google/gemini-embedding-001
SYMTRI_SUMMARY_MODEL=anthropic/claude-haiku-4.5
# Optional: raises GitHub API rate limits for server-side ingestion.
GITHUB_TOKEN=
